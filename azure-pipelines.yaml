# Node.js
# Build a general Node.js project with npm.
# Add steps that analyze code, save build artifacts, deploy, and more:
# https://docs.microsoft.com/azure/devops/pipelines/languages/javascript

trigger:
  branches:
    include:
     - release
     - staging
 
pool:
  vmImage: ubuntu-latest

steps:
- script: |
   sed -i 's/Question.json/Question-test.json/g' models/questionRepository.js
  condition: eq(variable['Build.SourceBranch'], 'refs/heads/staging')
  displayName: 'Change Question bank'

- task: NodeTool@0
  inputs:
    versionSpec: '10.x'
  displayName: 'Install Node.js'

- script: |
    npm install
    npm run build
  displayName: 'npm install and build'

- task: Docker@2
  displayName: Login to Docker Hub
  inputs:
    command: login
    containerRegistry: dockerRegistryServiceConnection2

- task: Docker@2
  condition: eq(variable['Build.SourceBranch'], 'refs/heads/staging')
  displayName: Build and Push
  inputs:
    command: buildAndPush
    repository: contosoRepository # username/contosoRepository for DockerHub
    tags: |
      staging
      $(Build.BuildID)

- task: Docker@2
  condition: eq(variable['Build.SourceBranch'], 'refs/heads/release')
  displayName: Build and Push
  inputs:
    command: buildAndPush
    repository: contosoRepository # username/contosoRepository for DockerHub
    tags: |
      latest
      $(Build.BuildID)      